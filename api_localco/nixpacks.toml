[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "nodejs-18_x"]

[start]
cmd = "composer install --no-dev --optimize-autoloader && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
